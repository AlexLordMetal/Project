@model ServicesApp.ViewModels.IdentityViewModels.ServiceProviderProfileViewModelManage
@{
    ViewBag.Title = "Your Profile";
}

<h2>@ViewBag.Title.</h2>

<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>Password</dt>
        <dd>
            [
            @if (Model.HasPassword)
            {
                @Html.ActionLink("Change your password", "ChangePassword", "Manage")
            }
            else
            {
                @Html.ActionLink("Create", "SetPassword", "Manage")
            }
            ]
        </dd>
        <dt>@Html.DisplayNameFor(m => m.ServiceProviderProfile.Name)</dt>
        <dd>
            @Html.DisplayFor(m => m.ServiceProviderProfile.Name)
        </dd>
        <dt>@Html.DisplayNameFor(m => m.ServiceProviderProfile.ConfirmDocs)</dt>
        <dd>
            @Html.DisplayFor(m => m.ServiceProviderProfile.ConfirmDocs)
        </dd>
        <dd>
            [
            @Html.ActionLink("Change", "UpdateProfile")
            ]
        </dd>
    </dl>
</div>